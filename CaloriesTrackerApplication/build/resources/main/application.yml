spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/calories_tracker
    username: postgres
    password: 128N8rwd76
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true